from db import xternityDB

from datetime import datetime

# def Insert_logs_from_file(type):
#     print()
    # with open(f'C:\\Users\\May\\Desktop\\externity\\app\\xg-{type}-1min-avg.log') as f:
    #         lines = f.readlines()
    #         for line in lines:
    #             db.planets.insert_one({
    #                 "name": line.split(",")[1].strip() ,
    #                 "value": float(line.split(",")[2].strip()) , 
    #                 "logTime": datetime.fromtimestamp(float(line.split(",")[0]))
    #                 }
    #         )

